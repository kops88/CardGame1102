import UE from "ue";
import { CardDef } from "../Blueprint/BPW/CardInstance/CardInstance";

export class ConfigUtils {

    static LoadCardInfo(cid: number): CardDef {
        let table = UE.DataTable.Load()
        
    }
}